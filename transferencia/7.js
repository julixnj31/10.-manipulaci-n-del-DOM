/**
 * Implementar la función hideEmptyState() para ocultar
 * el mensaje de “sin mensajes” cuando existan mensajes registrados.
 * 
 * Se utiliza classList.add("hidden") para agregar
 * la clase que oculta el elemento en pantalla.
 */

function hideEmptyState() {
    emptyState.classList.add("hidden");
}