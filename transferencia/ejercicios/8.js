/**
 * Implementar la función showEmptyState() para mostrar
 * nuevamente el mensaje de “sin mensajes” cuando no existan mensajes.
 * 
 * Se utiliza classList.remove("hidden") para remover
 * la clase que oculta el elemento en pantalla.
 */

function showEmptyState() {
    emptyState.classList.remove("hidden");
}