{
  "name": "servidor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}